# RF-DETR Keypoint (Apache 2.0) — the "light model" that watches the webcam.
rfdetr>=0.1.0
supervision>=0.21.0
# Capture + image handling.
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0
# Config + event output.
pyyaml>=6.0
# Keyframe recognition (known-person pHash compare, application-side).
imagehash>=4.3.0
# Face-ID embeddings (optional; default recognition method).
insightface>=0.7.3
onnxruntime>=1.16
# Ollama scene caption is used instead of a local HuggingFace model, so no
# torch/transformers dependency is needed here. The security app calls Ollama
# over HTTP when caption.enabled is true and the model is pulled.